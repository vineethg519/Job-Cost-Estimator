
<% console.log(estimatePartFloorings.query); %>
<div class="container">
 <h2>All entries</h2>

<p>There are  <%= estimatePartFloorings.query.length %> entries. <br/>
<a href="create.ejs">Add a new one.</a></p>

<table class="table" border="1">
    <thead>
      <tr>
           <th>Subtotal</th>
       <th>Actions</th>
    </tr>
    </thead>
    <tbody>
<% for (var i = 0; i < estimatePartFloorings.query.length; i++) { %>
         <tr>
            <td><%=  estimatePartFloorings.query[i].subtotal %></td>
            <td>
            <a href="estimatePartFlooring/delete"<%= estimatePartFloorings.query[i].id %>"> Delete </a> | 
            <a href="estimatePartFlooring/details"  <%= estimatePartFloorings.query[i].id %>"> Details</a> |
            <a href="estimatePartFlooring/edit" <%= estimatePartFloorings.query[i].id %>">  Edit </a> 
            
            </td>
         </tr>
         <% } %>
</tbody>
</table>
</div>
<!--This model is managed by Team 3-5
Sai Rama Rao Nayeni
AjithReddy Pati -->

