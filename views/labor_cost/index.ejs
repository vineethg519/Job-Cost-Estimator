
<% console.log(estimatePartLabors.query); %>
<div class="container">
 <h2>All entries</h2>

<p>There are  <%= estimatePartLabors.query.length %> entries. <br/>
<a href="create.ejs">Add a new one.</a></p>

<table class="table" border="1">
    <thead>
      <tr>
        <th>Subtotal</th>
       <th>Actions</th>
    </tr>
    </thead>
    <tbody>
<% for (var i = 0; i < estimatePartLabors.query.length; i++) { %>
         <tr>
            <td><%=  estimatePartLabors.query[i].subtotal %></td>
            <td><a href="estimatePartLabor/delete/<%=  estimatePartLabors.query[i]._id %>"> Delete </a> |
							<a href="estimatePartLabor/details/<%= estimatePartLabors.query[i]._id %>"> Details </a> |
							<a href="estimatePartLabor/edit/<%=    estimatePartLabors.query[i]._id %>"> Edit </a></td>
         </tr>
         <% } %>
</tbody>
</table>
</div>
<!--This view is managed by Team 3-10-->
<!--Vineetha Boppishetty-->
<!--Renu Kappara-->
