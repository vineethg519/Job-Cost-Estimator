
<% console.log(estimatePartFootages.query); %>
<div class="container">
 <h2>All entries</h2>

<p>There are  <%= estimatePartFootages.query.length %> entries. <br/>
<a href="create.ejs">Add a new one.</a></p>

<table class="table" border="1">
    <thead>
      <tr>
        <th>SqFt</th>
       <th>Actions</th>
    </tr>
    </thead>
    <tbody>
<% for (var i = 0; i < estimatePartFootages.query.length; i++) { %>
         <tr>
            <td><%=  estimatePartFootages.query[i].sqft %></td>
            <td>
           <a href="estimatePartFootage/delete"<%= estimatePartFootages.query[i].id %>"> Delete </a> | 
            <a href="estimatePartFootage/details"  <%= estimatePartFootages.query[i].id %>"> Details</a> |
            <a href="estimatePartFootage/edit" <%= estimatePartFootages.query[i].id %>">  Edit </a> 
            </td>
         </tr>
         <% } %>
</tbody>
</table>
</div>
<!-- This model is managed by Team 3-4 -->
<!-- Abhinav Ekka -->
<!-- Pradeep Vunnam -->
